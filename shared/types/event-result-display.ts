/**
 * Display information for a submissions of an event
 */
export type EventResultDisplay = {
    name: string,
    wcaId: string,
    best: string,
    average: string,
    solves: string[]
    place: number,
};